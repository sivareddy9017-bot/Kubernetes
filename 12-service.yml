apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    # Labels used as selectors
    project: roboshop
    environment: dev
    component: frontend
ports:
    - protocol: TCP
      port: 80
      targetPort: 80